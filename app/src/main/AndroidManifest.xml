<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blog">
        <activity
            android:name=".FeedForAdminActivity"
            android:exported="true"
            android:parentActivityName=".LoginActivity"
            android:label="Новости" />
        <activity
            android:name=".editNews"
            android:exported="true"
            android:label="Редактирование новости"/>
        <activity
            android:name=".AddNewsActivity"
            android:exported="true"
            android:label="Добавление новости"/>
        <activity
            android:name=".RegActivity"
            android:exported="true"
            android:label="Регистрация"/>
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Авторизация"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>